
class CPInstruction(object):

    def __init__(self, origin_hosts, objective_hosts, multipliers):
        self.origin_hosts = origin_hosts
        self.objective_hosts = objective_hosts
        self.multipliers = multipliers
